apiVersion:
kind: Pod 
metadata: 
  name: nginx
  labels:
    app: new_app
spec:  
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
      protocol: TCP 
  - name: tomcat
    image: tomcat
    ports:
    - containerPort: 8080
      protocol: TCP